import {
  expect, test,
} from 'vitest';
// Edit an assertion and save to see HMR in action

test('sample 1', () => {
  expect(true).toBe(true);
});

test('sample 2', () => {
  expect(false).toBe(false);
});
